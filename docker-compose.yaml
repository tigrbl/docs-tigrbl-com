services:
  docs-tigrbl-com:
    build: .
    container_name: docs-tigrbl-com
    restart: unless-stopped
    labels:
      site.host: docs.tigrbl.com
      site.kind: zdx-docs
    expose:
      - "80"
