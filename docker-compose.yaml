services:
  docs-tigrbl-com:
    build: .
    container_name: docs-tigrbl-com
    restart: unless-stopped
    labels:
      site.host: docs.tigrbl.com
      site.kind: zdx-docs
    ports:
      - "127.0.0.1:0:80"
